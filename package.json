{
  "name": "harald",
  "version": "0.0.1",
  "description": "Node wrapper for bluetoothctl",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "npm-watch",
    "prepack": "npm run build"
  },
  "author": "Siem Lasseel",
  "license": "ISC",
  "dependencies": {
    "node-pty": "^0.10.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sieem/harald.git"
  },
  "files": [
    "lib/**/*"
  ],
  "devDependencies": {
    "npm-watch": "^0.11.0",
    "typescript": "^4.4.3"
  },
  "watch": {
    "build": {
      "patterns": ["src"],
      "extensions": "ts"
    }
  }
}
